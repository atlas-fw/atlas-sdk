rootProject.name = "atlas-sdk"

// The following lines are for ease of development,
// you can remove them if you don't need them or don't know what they do
pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

